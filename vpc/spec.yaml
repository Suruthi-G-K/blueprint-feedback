name: "blueprint-feedback x Basic Example"
schema_version: "1.0.0"
description: "input output basic"
inputs:
  - name: prefix
outputs:
  - name: prefix
    value: $module.layer0.outputs.prefix
  - name: out
    value: $module.layer1.outputs.out0
settings:
  - name: TF_VERSION
    value: 1.1
modules:
  - name: layer0
    module_type: terraform
    source:
      source_type: github
      git:
        git_repo_url: "https://github.com/powellquiring/blueprint-feedback/tree/master/vpc/layer0"
    inputs:
      - name: prefix
        value: $blueprint.prefix
    outputs:
      - name: prefix
  - name: layer1
    module_type: terraform
    source:
      source_type: github
      git:
        git_repo_url: "https://github.com/powellquiring/blueprint-feedback/tree/master/vpc/layer1"
    inputs:
      - name: prefix
        value: $module.layer0.outputs.prefix
    outputs:
      - name: out0
